# Cross Cutting inventory

This file is generated from `product-model/areas/cross-cutting.json`. Edit the canonical product model, then run `npm run product:docs`.

| ID | Area | Feature | Status | Owner / contract |
|---|---|---|---|---|
| cross-cutting.internationalization | Internationalization | Typed, validated catalogs and English interface catalog | Implemented | packages/web/src/i18n; English is the only complete installed catalog. Application copy is catalog-validated, production surfaces inherit the root locale provider, and localized text is excluded from selectors and logic. Localized defaults become Article data only when author-editable content is created. |
| cross-cutting.accessibility | Accessibility | Semantic controls, focus treatment, keyboard navigation, and non-color cues | Implemented | packages/web/src/ui, packages/web/src/workspace/components/WorkspaceShell.tsx, packages/web/src/settings/ApplicationSettings.tsx, packages/web/src/settings/components/SettingRow.tsx, packages/web/src/settings/components/KeyBindingSettings.tsx; Workspace and Settings controls expose semantic labels, keyboard access, visible focus, and non-color status conventions. Accessibility behavior is derived from rendered controls and does not persist sensitive state. |
| cross-cutting.keyboard-control | Keyboard control | Shortcut hints, normalization, conflict detection, and command registration | Partial | packages/shared/src/cross-cutting/key-bindings.ts, packages/web/src/key-bindings; Key bindings normalize, resolve defaults, identify conflicts, and register commands. Author-selected overrides persist in Application Settings. |
| cross-cutting.notifications | Notifications | Shared success and error provider with viewport | Implemented | packages/web/src/notifications; Recoverable failures are presented through a shared notification API without exposing sensitive values. Notifications are transient renderer state. |
| cross-cutting.data-validation | Data validation | Shared contracts, HTTP constants, and transport-boundary validation | Implemented | packages/shared/src, packages/server/src/presentation, packages/server/src/application, packages/server/src/infrastructure; Shared validation and HTTP constants reject malformed transport data before it reaches application behavior; provider-specific prompt roles are normalized at the provider boundary. Invalid provider or transport payloads are not persisted as valid application data. |
| cross-cutting.privacy | Privacy | Server-side secrets, local materials, minimal context, redacted diagnostics, and storage opt-in | Implemented | packages/server/src/infrastructure/configuration/config.ts, packages/server/src/application/editorial, packages/web/src/notifications; Secrets remain server-side, including custom-named values loaded from the project .env file before local-service startup; materials remain local, only necessary request context is sent, and provider response storage requires explicit opt-in. Private content is not logged or traced by default; provider-side response storage is disabled unless enabled. |
| cross-cutting.database-lifecycle | Database lifecycle | SQLite persistence and Article schema transition | Implemented | packages/server/src/infrastructure/persistence/database.ts, packages/server/src/infrastructure/persistence; Startup performs the one-time legacy Documents-to-Articles schema transition, then preserves the resulting Article database. SQLite remains the durable local data store after migration. |
| cross-cutting.assistant-persistence | Assistant persistence | Local Article conversation records | Implemented | packages/server/src/infrastructure/persistence/repositories/assistant-repository.ts, packages/server/src/presentation/routes/assistant-route.ts; Greetings, messages, request scope, completions, and safe failure or cancellation status are isolated per Article; author messages retain resolved skills and offsets. Conversation records are local and cascade with their Article; fixed application messages use stable templates while author and model content remain authored text. |
| cross-cutting.data-protection-activity-center | Data-protection activity center | Persistent application-level checkpoint, Revision, backup, and future sync activity | Deferred | AGENTS.md; A future activity center is distinct from popup-notification history and does not replace contextual recovery. Deferred until the post-MVP design and storage contract are approved. |
| cross-cutting.deferred-mvp-boundary | Deferred MVP | Accounts, sync, teams, analytics, direct publishing, imports, exports, additional providers, and alternate clients | Deferred | AGENTS.md; These capabilities remain outside the validated MVP editorial loop. No persistence or network surface is introduced for deferred MVP work. |
