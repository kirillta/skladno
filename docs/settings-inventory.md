# Application Settings inventory

The authoritative inventory and persistence tracker is in the auto-discovered `skladno-settings` Codex skill: `references/settings-inventory.md`.

Interface locale infrastructure is complete. English is the only installed complete interface catalog; future locales are selectable only after catalog validation and review.
