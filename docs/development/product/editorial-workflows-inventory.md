# Editorial Workflows inventory

This file is generated from `product-model/areas/editorial-workflows.json`. Edit the canonical product model, then run `npm run product:docs -- editorial-workflows`.

| ID | Feature | Status | Contract | Persistence | Related capabilities |
|---|---|---|---|---|---|
| editorial-workflows.assistant-foundation | Durable local conversation and built-in skill registry | Implemented | Each Article has one persisted greeting and a durable local conversation for six built-in Skills. Quick Actions insert a Skill starter and Send remains the sole request trigger. The live Assistant uses a six-step bounded catalog-tool loop with catalog-owned validation, Skill discovery, conversation history, stale-Revision rejection, and one primary generated artifact. It can inspect Article context, Revisions, artifacts, publishing guidance, and Style Corpus state. A separate structured intent check using the configured Text Generation Model accepts explicit exact action requests in the Author's language and fails closed before server validation; orchestrating model tool calls and general Article conversation cannot authorize mutation. Selection-scoped requests remain limited to their selected text and cannot call whole-Article capabilities. Revision-bound requests retain their selected or resolved Skill, completed messages, execution metadata, and review artifacts. Fact Check shows extracted claims as pending or checked, excluding unchanged supported claims. Talking Points prioritizes the active selection, then Author message, then Article. Narrative Draft follows Author direction, develops only the selected source when present, and treats character limits as guidance. | Assistant messages, requests, completed artifacts, and minimal per-capability execution history persist locally. Execution history retains only capability ID, status, request ID, base Revision, and timestamps; Workflow Stage persistence is retired and legacy values remain untouched. | — |
| editorial-workflows.assistant-panel-streaming | Streamed responses, progress, cancellation, and error recovery | Implemented | Assistant requests stream typed status, renderer-safe capability activity, and response events. Conversational output reveals completed Markdown blocks without rewriting earlier blocks; review artifacts transition directly from the temporary stream to a short result card with an explicit action to their owning Workspace View, and the current View stays unchanged until the Author follows that action. Streaming follows only when the Author is already near the latest output; scrolling away exposes a return-to-end control, while completion brings the outcome control into view unless focus remains in the Assistant. Failed and cancelled requests expose a Retry action that replays only the local persisted input; an unavailable AI connection also offers Application Settings. Incomplete streams do not modify an Article. | Only valid completed request output becomes local message or review-artifact state. | — |
| editorial-workflows.thesis-composition | Thesis-to-narrative composition | Implemented | The server creates a composition Proposal from author-supplied theses; it remains reviewable until acceptance. | Completed proposal artifacts are local and revision-bound. | — |
| editorial-workflows.flow-revision | Flow revision | Implemented | Flow revision generates a full-text Proposal rather than silently editing the Article. | Completed proposal artifacts are local and revision-bound. | — |
| editorial-workflows.fact-check-request | Fact-check request | Implemented | A fixed server-side fact-check workflow creates advisory findings with per-Fact freshness across Revisions; unchanged Facts and accepted findings remain active. | Findings and citations are stored locally without changing Article text. | — |
| editorial-workflows.style-review | Style review | Implemented | Style review uses only the current compact, explicitly rebuilt profile plus Article-specific manual rules; raw style samples remain local and review findings retain freshness state with the grounded trait or rule that supports each finding. | Raw style corpus and derived review artifacts stay local. | — |
| editorial-workflows.translation-request | Translation request | Implemented | Translation requests prepare a translated Article title and linked Article content together; the source Article is never overwritten. | Translation review metadata is local and source linkage remains recoverable. | — |
| editorial-workflows.proposal-lifecycle | Summarized diff review, bulk acceptance or rejection, fallback, and stale protection | Implemented | Authors may review non-authoritative Interface-locale summaries, decide every change locally, apply the accepted subset in one Revision, accept a whole Proposal in one Revision, or dismiss stale and zero-change Proposals without Article changes. Fact corrections preserve blank-line formatting. Summary failure does not block review. Stale and preservation warnings precede labelled change navigation and diff; stale or unreliable Proposals cannot be partially accepted. Whole-Proposal fallback discloses its change count. | Completed Proposal content, summaries with their Interface locale, and the base Revision remain locally recoverable in the Proposal artifact; acceptance creates one immutable Revision and rejection leaves Article history unchanged. | workspace.proposal.explicit-acceptance |
| editorial-workflows.ai-safety | Explicit request and explicit proposal approval | Implemented | AI operations require an author request; generated text remains separate until explicit author acceptance, with stale-acceptance conflicts handled by the application service. | Only accepted content creates a Revision. | — |
| editorial-workflows.ai-connections | Named AI connections and environment-variable references | Partial | Authors configure named AI connections, active selection, testing, and environment-variable names without exposing secret values. | Connection metadata and active selection persist locally; credentials remain environment-provided. | settings.ai-connections |
| editorial-workflows.ai-model-selection | Refreshed model list and purpose-specific model preferences | Implemented | The default model and built-in-skill overrides determine editorial requests, each with its own reasoning effort. An optional text-generation model handles supporting text such as Proposal summaries, has its own reasoning effort, falls back to the default model, and disables provider-side storage. | Model preferences persist locally; refreshed provider model lists are not persisted. | settings.model-preferences, settings.available-model-list |
