# History And Publishing inventory

This file is generated from `product-model/areas/history-and-publishing.json`. Edit the canonical product model, then run `npm run product:docs -- history-and-publishing`.

| ID | Feature | Status | Contract | Persistence | Related capabilities |
|---|---|---|---|---|---|
| history-and-publishing.revision-history | Immutable Revision timeline and read-only preview | Implemented | The timeline and compact Status Bar selector distinguish initial, manual, AI-assisted, and restored Revisions. Conversational Assistant edits use the AI-assisted presentation and retain request and reply provenance. When the first immutable Revision is empty, the UI labels it Empty Revision and labels the second saved Revision Initial Revision; both remain selectable. New author, accepted-Proposal, and Assistant-edit Revisions use a concise configured-model summary of the changed context in the interface locale as their accessible history title, with a deterministic local fallback when generation fails or is cancelled. Selecting a Revision is local-only, opens a read-only preview, and does not alter the Article or Draft; restoration keeps the existing confirmation flow, and revision conflicts use an application contract rather than persistence types. | Revisions are immutable local records ordered by creation. New content-changing Revisions persist their generated or fallback description only after completion; existing Revision records remain unchanged. | — |
| history-and-publishing.revision-restore | Restore a prior Revision | Implemented | Restoring a prior Revision creates a new Revision and never rewrites history; uncommitted Drafts require an explicit choice, and stale revision conflicts remain application-owned. | Restoration appends a new immutable local Revision. | — |
| history-and-publishing.style-corpus | Versioned local writing samples and explicit compact style profile rebuild | Implemented | Authors manage included local samples, explicitly confirmed immutable Revision snapshots, and global rules; duplicate text is rejected and removal requires confirmation. For explicitly submitted text without a source name, the configured text-generation model names the sample while Submit shows loading. Input changes leave the current profile visible but outdated until explicit rebuild creates a version and Assistant service message. The profile identifies exact contributors and rebuild time. | Raw samples, immutable Article Revision provenance, global rules, Article-specific rules, generated source names, and append-only compact profile versions remain local; Style Profile exposes a short local sample excerpt for author recognition. | — |
| history-and-publishing.fact-check-findings | Revision-tied findings, citations, uncertainty, and stale indication | Implemented | Findings remain advisory, Revision-tied, cited, uncertain, stale-aware, and actionable from Fact Check or Assistant. Fact Check shows status badges, selected-finding navigation, concise Revision numbers, and independently scrolling navigation and detail panes. Accepting a finding records only local advisory resolution and creates no Revision. Accepting a correction Proposal marks covered findings corrected or removed; remaining findings become stale with the new Revision. | Findings and citations persist locally without modifying Article text; focused updates to other editorial artifacts do not replace or detach their citations. | — |
| history-and-publishing.translations | Linked translation Articles with source freshness protection | Implemented | Translations are independently recoverable linked Articles. The workspace starts configured target-language requests through Assistant messages, restores the latest completed proposal per language, and supports language navigation. Each completed proposal contains the translated title and body; source and translation titles remain independent. Source Articles list linked translations; each translation links back and identifies the source Revision. Source and translation appear with full language names side by side, with paragraph and Markdown-list alignment where possible, plus visible protected-span validation and stale-source warnings. A proposal with changed or missing protected content cannot create a translation Article. The selected translation shows advisory character guidance for the Article's selected publishing profile. Created translations open in the editor for author-controlled Drafts and Revisions; stale proposals cannot create them. Authors can explicitly reject generated output after confirmation, which removes it from approval without changing the source or accepted translation history. | Completed and rejected translation proposals persist in local Assistant artifacts; Source Article and Revision linkage persist on the independently recoverable translation Article. | — |
| history-and-publishing.publishing-copy | Explicit Markdown and plain-text copying with character guidance | Implemented | The Status Bar main action copies current Article Markdown, while its menu offers Markdown or deterministically prepared plain text. Article-specific profile guidance, including configured custom profiles, remains available beside it. Copying is explicit and direct publishing is not performed. | Copied output is derived from the local Article and locally persisted profile data. | — |
| history-and-publishing.publishing-profiles | Named profiles with configurable limits | Implemented | Authors choose a predefined default for new Articles and configure named custom non-negative limits with stable IDs. Each Article independently selects Default, No restrictions, LinkedIn Post, LinkedIn Article, or a custom profile for advisory guidance. | The default profile and custom name/limit persist locally in app_settings.publish-limit-profile; Article-specific selection persists locally. | settings.publishing-profiles |
