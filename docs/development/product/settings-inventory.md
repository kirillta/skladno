# Settings inventory

This file is generated from `product-model/areas/settings.json`. Edit the canonical product model, then run `npm run product:docs -- settings`.

| ID | Feature | Status | Contract | Persistence | Related capabilities |
|---|---|---|---|---|---|
| settings.general-preferences | Appearance, locale, and date/time/time-zone | Implemented | General Settings persists a System, Light, or Dark appearance preference; the shared resolver maps System to the current device appearance for visual application. It also supports the Interface locale, device-local or explicit IANA time zones, device-local date and time formatting, and explicit date and time formats. Revision History honors saved formatting while generated supporting UI text uses the Interface locale independently of Article language. | app_settings.application-general | — |
| settings.telemetry-consent | Optional packaged-app telemetry consent | Partial | During public beta, the packaged Electron app shows a localized, keyboard-accessible default-on switch for a finite pseudonymous diagnostic and usage event contract, plus an enabled-only copyable Telemetry ID for deletion requests. The server emits one finite terminal event after persisted Assistant or direct Editorial completion, or after their failure/cancellation; staged Assistant work is not counted separately. Proposal acceptance captures consent before Revision persistence and emits only after that persistence succeeds. The workspace batches only actual Draft save/discard persistence attempts that began with telemetry enabled into a 30-second in-memory aggregate; no-op checkpoints are excluded. Electron emits one terminal event for each manual, deletion-protective, restore-recovery, or update-recovery SQLite snapshot, plus finite startup, renderer, and child-process failures when the main process remains alive. Main validates each IPC request and event, includes only trusted platform, architecture, and OS-version fields, keeps identity and consent outside Article data, clears both and all in-memory work on opt-out, and treats missing delivery configuration as disabled. Delivery uses an in-memory cap of 100 events per minute and 100 retained events, expires events after five minutes, retries transient failures once without following redirects, and never blocks editorial work. Browser and development runtimes cannot send telemetry. | Electron userData/runtime-settings.json only; it is excluded from SQLite Article data, backups, and restore staging. | — |
| settings.key-bindings | Configurable application shortcuts | Implemented | Settings can persist command overrides and expose conflict-aware shortcut configuration. The Editorial Assistant also supports choosing Enter or Ctrl+Enter as the send key independently of command overrides. | app_settings.application-key-bindings and app_settings.application-general | — |
| settings.ai-connections | AI connections and activation | Implemented | Authors choose OpenAI, OpenCode Zen, Anthropic, Google Gemini API, xAI Grok, or DeepSeek before adding an environment-variable connection. In Electron, they first choose an environment variable or managed API key, which Skladno verifies then stores in Windows Credential Manager. Connections are active by default; authors can activate or deactivate each one independently, rename it, test it, or confirm deletion without exposing secret values. | app_settings.application-ai-connections | — |
| settings.model-preferences | Default, supporting-text, per-skill, reasoning, and favorite-model preferences | Implemented | Each selected model records its active connection as well as its provider model ID, so duplicate model names remain routable and an OpenCode Zen model can disclose its underlying vendor. Default, supporting-text, per-skill, reasoning, and favorite-model preferences apply globally. An optional text-generation model handles supporting text such as Proposal summaries and falls back to the default. Authors can save favorite available models, search the available list, and filter it by recognized vendor. Low, medium, and high reasoning-level controls are offered only for selected OpenAI models. Presentation receives Settings through the application service, persistence uses focused repositories, and legacy preferences migrate to their formerly active connection without exposing unknown keys. | app_settings.application-model-preferences | — |
| settings.available-model-list | Available model list refresh | Implemented | Opening AI Settings loads the current model lists for every active OpenAI, OpenCode Zen, Anthropic, Google Gemini API, xAI Grok, or DeepSeek connection; authors can refresh them again to retry or retrieve provider changes. Each returned model retains the connection that supplied it. The OpenAI catalog admits only the gpt-5.5 and gpt-5.6 model families; Zen retains its complete returned catalog. A refreshed list is not proof that every Editorial Operation is supported. | Server-side provider refresh only; the resulting list is not persisted. | — |
| settings.publishing-profiles | Publishing profiles and language defaults | Partial | Default, No restrictions, LinkedIn Post, LinkedIn Article, and named custom non-negative limits provide guidance; the selected default applies to new Articles while Articles retain independent selection. Custom profiles have stable IDs, appear before add controls, and require confirmed removal; removing the selected default resets to Default. Settings persists one default Article language and deduplicated translation languages excluding it. Translate creates reviewable proposals for every configured target. | app_settings.publish-limit-profile | — |
| settings.backup-policy | Browser-selected backup folder, schedule, and retention | Implemented | In supported web browsers, authors explicitly choose a local backup folder. The browser writes consistent SQLite snapshots there, lists compatible snapshots for explicit restore, and reloads after the loopback service safely replaces active data. Daily startup writes at most one automatic snapshot when the previously selected folder remains permitted, and retention removes only automatic snapshots. | app_settings.application-backup-policy plus the browser's local folder permission handle; snapshots exclude environment variables and API keys. | — |
| settings.delete-local-data | Explicit Electron local-data deletion | Implemented | In Electron, an author can request deletion of the active resolved Skladno data directory through a no-argument native operation. The native confirmation explains the irreversible scope and always shows a Create a backup before deletion checkbox, checked only when a separate backup directory is configured. A requested backup must succeed before deletion continues. It cancels without changes, closes SQLite and background work before deletion, then exits only after successful removal. The renderer cannot supply a filesystem path. | The confirmed active local data directory is removed. Installer and uninstaller behavior remains data-preserving. | — |
| settings.restore-local-backup | Explicit local-backup restoration | Implemented | An author chooses a SQLite snapshot from the configured backup folder and explicitly confirms replacement. Browser restore uploads the selected snapshot only to the loopback service, which closes and reopens the local application before the browser reloads. Electron uses a staged native restart. The renderer cannot provide an active-data path. Skladno retains a recovery snapshot, validates the snapshot before and after replacement, and rolls back once when restored data cannot open. | The selected snapshot replaces the active SQLite database only during restart. A private recovery snapshot remains outside the active database. | — |
| settings.preview-updates | Author-controlled unsigned Windows updates | Implemented | About Settings asks the author in a confirmation dialog to allow GitHub update network access before any discovery request. After consent, it exposes automatic checks, a stable-only or prerelease-inclusive channel preference, explicit download, guarded restart, release notes, and recovery guidance. An available update appears in the Article Library utility area and opens About Settings with Updates focused. Browser builds omit the group; Electron development builds expose the persisted preferences but no discovery or download actions. | The desktop runtime file stores the network-permission, automatic-check, and prerelease-channel preferences plus non-content update recovery metadata. | — |
| settings.about | Repository and license | Implemented | About Settings provides compact external links to the GitHub source repository, issue tracker, and MIT License. It keeps these details read-only and opens links in the system browser. | Static application metadata; no author data is stored. | — |
