# Settings inventory

This file is generated from `product-model/areas/settings.json`. Edit the canonical product model, then run `npm run product:docs -- settings`.

| ID | Feature | Status | Contract | Persistence | Related capabilities |
|---|---|---|---|---|---|
| settings.general-preferences | Appearance, locale, and date/time/time-zone | Implemented | General Settings supports the Interface locale, device-local or explicit IANA time zones, device-local date and time formatting, and explicit date and time formats. Revision History honors saved formatting while generated supporting UI text uses the Interface locale independently of Article language. | app_settings.application-general | — |
| settings.key-bindings | Configurable application shortcuts | Implemented | Settings can persist command overrides and expose conflict-aware shortcut configuration. | app_settings.application-key-bindings | — |
| settings.ai-connections | OpenAI connections and active connection | Implemented | Authors can add connections, prevent duplicate environment-variable names, choose an active connection, test it, and confirm deletion without exposing secret values. | app_settings.application-ai-connections | — |
| settings.model-preferences | Default, supporting-text, and per-skill models | Implemented | A default model and per-skill overrides apply to six built-in skills. An optional text-generation model handles supporting text such as Proposal summaries and falls back to the default. Presentation receives Settings through the application service, persistence uses focused repositories, and legacy preferences normalize on read without exposing unknown keys. | app_settings.application-model-preferences | — |
| settings.available-model-list | Available model list refresh | Implemented | Opening AI Settings or changing the active OpenAI connection loads its available model list for model selection; authors can refresh the list again to retry or retrieve provider changes. | Server-side OpenAI refresh only; the resulting list is not persisted. | — |
| settings.publishing-profiles | Publishing profiles and language defaults | Partial | Default, No restrictions, LinkedIn Post, LinkedIn Article, and named custom non-negative limits provide guidance; the selected default applies to new Articles while Articles retain independent selection. Custom profiles have stable IDs, appear before add controls, and require confirmed removal; removing the selected default resets to Default. Settings persists one default Article language and deduplicated translation languages excluding it. Translate creates reviewable proposals for every configured target. | app_settings.publish-limit-profile | — |
| settings.active-data-directory | Active data directory | Partial | The active local data directory and server-only environment values are determined at startup; project .env values are loaded before the local service starts and Settings displays the active data directory. | Startup configuration rather than a Settings-managed value. | — |
| settings.backup-policy | Backup destination, schedule, and retention | Partial | Authors can save a backup destination, schedule, and retention policy. | app_settings.application-backup-policy | — |
