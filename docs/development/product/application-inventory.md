# Application inventory

This file is generated from `product-model/areas/application.json`. Edit the canonical product model, then run `npm run product:docs -- application`.

| ID | Feature | Status | Contract | Persistence | Related capabilities |
|---|---|---|---|---|---|
| application.local-service-client | Local React application with loopback service and typed client | Implemented | The renderer uses typed HTTP and Electron application-client operations, including supporting Proposal summaries, against local application services; credentials and privileged model operations remain server-side. | No renderer access to secrets, files, or SQLite. | — |
| application.health-and-recoverable-errors | Health reporting and recoverable client errors | Implemented | Health and stable error codes allow the renderer to present recoverable failures without exposing secrets or full Article bodies. | Errors are transient UI state; sensitive values are not included in the transport contract. | — |
| application.workspace-and-settings-screens | Separate Editorial Workspace and Application Settings screens | Implemented | Application Settings is a workspace-level screen and never belongs to Article Revision history. | Screen selection is renderer state; Application Settings persist through their dedicated service contracts. | — |
| application.quick-start-tutorial | Non-blocking first-run quick start | Implemented | A compact first-run dialog explains Article writing, advisory Assistant output, and model-key setup without blocking editing. It directs an Author without a usable connection to AI Settings and can be reopened from About Settings. | Browser storage contains only the versioned skladno.quick-start.v1 completion marker; no Article, Settings, or credential data is stored. | — |
| application.local-first-single-user | Single-user, local-first operation | Implemented | Articles, Draft checkpoints, materials, style samples, model preferences, and completed Proposal summaries persist in local SQLite storage through focused domain-specific repositories wired at local-service startup; no aggregate repository facade is required. | SQLite is the active data store; private author data is not delegated to a remote application database. | — |
| application.electron-windows-preview | Secured Electron desktop client for Windows | Implemented | The unsigned Windows 11 x64 application runs the existing React application in one context-isolated, sandboxed Electron window without a native menu bar. Every former menu action is a configurable Key Binding: workspace actions run in the renderer, and editing, window, close, quit, and update actions cross only as allowlisted native command IDs. CI publishes stable tags as GitHub releases and preview tags as prereleases, each with the installer, RELEASES manifest, and full package. Its Squirrel installer creates Desktop and Start menu shortcuts. The main process composes local application services and exposes only allowlisted typed IPC operations. GitHub release discovery is optional, requires the author's stored network permission, and runs at every startup and then daily while the application remains open; the author chooses stable-only or prerelease-inclusive discovery, while download and restart remain explicit. Restart and update requires a Draft checkpoint and verified SQLite snapshot before native Squirrel apply. | The desktop client uses the existing user-local .skladno SQLite data. Runtime settings retain the network-permission, automatic-check, and prerelease-channel preferences, staged version, prior version, and one recovery snapshot reference without Article content. Install, upgrade, reinstall, and uninstall do not delete or rewrite that directory. | — |
| application.electron-linux-preview | Secured Electron desktop client for Debian-based distributions | Partial | The unsigned Debian x64 application runs the existing context-isolated, sandboxed Electron workspace and local services from one Debian package. Ubuntu 22.04 x64 is the release-validation baseline; compatible Debian-based distributions may install the same package. It preserves Article, Draft, Revision, backup, credential, renderer-isolation, single-window, native-dialog, and shutdown guarantees. After author-approved GitHub access, About Settings can show the newest matching Debian release and open its release notes. Package-manager upgrades or manual .deb reinstalls remain author-controlled; Linux exposes no in-app download or apply action. | Electron runtime state uses Linux userData while Article data remains in the resolved user-local .skladno directory. Install, upgrade, reinstall, and uninstall do not delete or rewrite author data. | — |
| application.desktop-responsive-shell | Desktop-ready responsive shell | Partial | The desktop shell supports collapsed panels, responsive workspace layout, and a prominent non-blocking warning with a direct AI Settings action whenever no active connected AI connection is available. | Workspace layout and proposal-advisory dismissal preferences are persisted locally in browser storage; AI connection readiness is read from application settings. | — |
