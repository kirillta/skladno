# Application inventory

This file is generated from `product-model/areas/application.json`. Edit the canonical product model, then run `npm run product:docs`.

| ID | Area | Feature | Status | Owner / contract |
|---|---|---|---|---|
| application.local-service-client | Application shell | Local React application with loopback service and typed client | Implemented | packages/web/src/App.tsx, packages/web/src/application-client.ts, packages/server/src/presentation/server.ts, packages/server/src/application, packages/shared/src; The renderer uses a typed application client against the local Node.js service; credentials and privileged operations remain server-side. No renderer access to secrets, files, or SQLite. |
| application.health-and-recoverable-errors | Application shell | Health reporting and recoverable client errors | Implemented | packages/server/src/presentation/routes/health-route.ts, packages/web/src/application-client.ts, packages/web/src/notifications; Health and stable error codes allow the renderer to present recoverable failures without exposing secrets or full Article bodies. Errors are transient UI state; sensitive values are not included in the transport contract. |
| application.workspace-and-settings-screens | Application shell | Separate Editorial Workspace and Application Settings screens | Implemented | packages/web/src/App.tsx, packages/web/src/workspace/EditorialWorkspace.tsx, packages/web/src/settings/ApplicationSettings.tsx; Application Settings is a workspace-level screen and never belongs to Article Revision history. Screen selection is renderer state; Application Settings persist through their dedicated service contracts. |
| application.local-first-single-user | Application shell | Single-user, local-first operation | Implemented | packages/server/src/infrastructure/persistence, packages/server/src/application, packages/server/src/presentation/server.ts; Articles, Draft checkpoints, materials, style samples, and settings persist in local SQLite storage through domain-specific repositories wired at local-service startup. SQLite is the active data store; private author data is not delegated to a remote application database. |
| application.desktop-responsive-shell | Application shell | Desktop-ready responsive shell | Partial | packages/web/src/workspace/components/WorkspaceShell.tsx, packages/web/src/workspace/EditorialWorkspace.tsx; The desktop shell supports collapsed panels and responsive workspace layout. Workspace layout preferences are persisted locally in browser storage. |
